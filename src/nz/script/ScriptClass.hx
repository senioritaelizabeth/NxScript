package nz.script;

/**
 * @deprecated ScriptClass was renamed to NxProxy. Update your imports.
 * This typedef exists so old code doesn't immediately break.
 * It will be removed at some point. That point is TBD.
 */
@:deprecated("ScriptClass has been renamed to NxProxy")
typedef ScriptClass = NxProxy;
